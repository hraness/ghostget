{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The local main window can send only the typed Ghostget control protocol.",
  "windows": ["main"],
  "permissions": ["allow-control-request"]
}
