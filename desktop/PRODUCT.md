# Ghostget control panel

This native companion lets a person connect accounts, inspect capabilities,
manage private vault items, integrations and permissions, approve bounded requests, inspect web
activity, and copy setup instructions for their own agent. The agent primarily
operates the existing CLI. The person intervenes occasionally.

The explicit brief sets macOS first, Paper appearance, five main navigation
destinations and Agent setup, readable provenance, honest connection states and no agent/chat runtime.
Activity is a searchable, filtered virtual table with infinite keyset loading.
Accounts starts with common services and an optional browser-hint discovery flow.
Connection details and uncommon filters use disclosures. The white ghost emoji
identifies the app across native, website, and inert previews. The implementation
inherits the site's existing Paper visual authority.
